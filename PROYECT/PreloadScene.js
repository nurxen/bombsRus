class PreloadScene{
    preload() {
        // Carga de recursos
        // this.load.image(‘id’, ‘path’);
    }
    create() {
        // Inicializa la escena
    }

    update(time, delta) {
        // Lógica de actualización aquí
        // 'time' es el tiempo total desde el inicio del juego
        // 'delta' es el tiempo transcurrido desde la última actualización
    }
}